import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'integration-test-v14',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
